[Settings]
# Point to the raw URL of distros.yaml on GitHub, or a local file path
yaml_source = distros.yaml

[ExternalScripts]
# Optional: Provide full paths if scripts aren't in system PATH or named differently.
# If omitted, script assumes 'download-windows-esd.py' and 'windows-esd-to-iso.sh' (or .cmd) are executable via PATH.
# --- CORRECTED PATHS ---
download_script_path = /home/mikkel/repos/iso-list/download-windows-esd
convert_script_path = /home/mikkel/repos/iso-list/windows-esd-to-iso
